import 'package:flutter/cupertino.dart';

const padH16 = EdgeInsets.symmetric(horizontal: 16);
const padAll16 = EdgeInsets.all(16);
const padAll10 = EdgeInsets.all(10);
const padH16V12 = EdgeInsets.symmetric(horizontal: 16, vertical: 12);
